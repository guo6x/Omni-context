# Dependencies
**/node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build outputs
dist/
out/
.next/
*.tsbuildinfo
**/dist/
**/out/
**/.next/
**/target/
**/build/

# ML model weights — re-downloadable HuggingFace/Tesseract artifacts.
# Too big for git (multilingual-e5-small onnx is 113MB, over GitHub's 100MB limit).
brain-server/models/

# Database
*.db
*.db-shm
*.db-wal
data/
**/data/*.db

# Tauri & Rust
desktop-daemon/src-tauri/target/
desktop-daemon/src-tauri/Cargo.lock
**/Cargo.lock
!desktop-daemon/src-tauri/Cargo.lock
# staging dir 由 scripts/package-all.js 生成，仅供 Tauri 打包用。
# 保留说明文件，让全新 checkout 中的 Tauri resource glob 也能被 cargo check 校验。
desktop-daemon/src-tauri/brain-server/*
!desktop-daemon/src-tauri/brain-server/README.md
desktop-daemon/brain-server/
desktop-daemon/brain-server-dist/

# Mobile (Expo/React Native)
mobile-app/.expo/
mobile-app/web-build/
**/ios/Pods/
**/android/.gradle/
**/android/app/build/

# IDE & OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Environment & Logs
.env
.env.*
!.env.example
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Tauri updater signing keys (private key = secret, never commit)
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
tauri_priv.key
desktop-daemon/.tauri-signing/

# Package-manager and cloud credentials
.npmrc
.yarnrc
credentials.json
service-account*.json
*credentials*.json

# Local secrets (generated per machine, never commit)
*-token.txt
local-token.txt
pair-code.txt
**/local-token.txt
**/pair-code.txt
*.token
*.secret

# Claude Code per-machine permission allowlist
# Auto-appends executed Bash commands as patterns, which can include API keys
# from env-var-prefixed test invocations. NEVER commit.
.claude/
.claude/settings.local.json

# Temporary files
*.tmp
*.temp

.antigravitycli/
.codegraph/

# External benchmark material must remain outside Git and outside this worktree.
external-data/
heldout-data/
locomo10.json
longmemeval*.json
!external-eval/fixtures/longmemeval-generation-12.json
trajectory_screenshots/
external-eval/runs/

# Local audit model cache is multi-gigabyte; pinned runtime models live on D:.
docs/embedding-audit-v1/evidence/model-cache/
