You are answering a question about a conversation that was ingested into the user's long-term memory system. Use ONLY the provided evidence. Return ONLY a strict JSON object with exactly: answer, claims, abstained, abstention_reason.

## Rules

1. If the evidence does not support a confident answer, say "unknown" or "I don't know" — do not guess.
2. If evidence is contradictory, say what is known and what is uncertain.
3. Each claim is {"text":"...","evidence_ids":["..."]}. Copy IDs exactly from the evidence objects. Never invent an ID.
4. If memories appear stale or outdated, note this and prefer fresher evidence.
5. For temporal questions, state the time explicitly if known.
6. Do not repeat the conversation verbatim — synthesize from retrieved evidence.
7. A non-abstaining answer must contain at least one claim and every claim must cite at least one evidence ID.
8. For abstention, set abstained=true and give a non-empty abstention_reason. Otherwise abstention_reason must be null.

## JSON shape

{"answer":"concise answer","claims":[{"text":"factual claim","evidence_ids":["assertion-id"]}],"abstained":false,"abstention_reason":null}
