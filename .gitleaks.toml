[extend]
useDefault = true

[allowlist]
description = "Cognitive benchmark fact keys mistaken for generic API keys"
regexTarget = "secret"
regexes = [
  '''^(current_project_phase|historical_phase_[1-6]|cycle_3_transition)$''',
]
