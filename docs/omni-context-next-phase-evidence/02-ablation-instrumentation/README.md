# Ablation instrumentation

Source of truth: `codex/omni-v3.1-strict-ablations`. The final instrumentation manifest and tests are imported only after the internal run ends so the attested product worktree remains immutable during execution.
