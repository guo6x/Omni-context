# Offline strict-ablation fixture

The synthetic fixture contains duplicate graph relations, support-note noise, current/historical and invalidated facts, conflicting updates, multiple agents, and multiple query slots. All four conditions ran with zero provider calls. Each ablation produced a distinct mechanism signature while the default path stayed equivalent to the frozen helpers.

Machine outputs are stored beside this file. The fixture is diagnostic only and is not a benchmark score.
