# Development-35 strict ablations

Internal results will be integrated only from the completed 35-by-4 interleaved run. A partial attempt invalidated by runtime HEAD attestation is retained separately and never pooled with the completed run.
