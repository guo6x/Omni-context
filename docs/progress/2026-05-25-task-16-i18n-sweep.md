# Task 16: 全量国际化扫描 — 进度记录

## 目标

把 desktop-daemon/src/ 下所有 .tsx/.ts 文件里的硬编码中文字面量迁移到 i18n。

## 改动清单

### 源文件改动

| 文件 | 改动 |
|------|------|
| components/ConfirmDialog.tsx | 导入 useTranslation，默认 confirmText/cancelText 走 t()，busy 文案走 t() |
| components/GraphViewer.tsx | relativeTime 移入组件内使用 t()；aria-label 走 t() |
| components/HardwarePairingPanel.tsx | formatRelative 移入组件内使用 t() |
| components/SearchPalette.tsx | decisionContextError 判断 i18n key 前缀走 t() |
| components/SettingsPanel.tsx | 语言按钮"中文"->t()；诊断详情全量走 t()；MCP 兜底卡片走 t() |
| components/OnboardingWizard.tsx | 删除 hardcoded fallback 字符串 |
| app/page.tsx | 删除 drag_drop.confirm_title 的 hardcoded fallback |
| hooks/useSearchMemory.ts | decisionContextError 改为 i18n key |
| hooks/useSettings.ts | DEFAULT_SHORTCUTS name/description/category 改为 i18n key 字符串 |
| lib/llm-presets.ts | speed/cost/name 改为 i18n key 字符串 |
| lib/mcp-clients.ts | 所有中文描述/reload_hint/steps 改为 i18n key 字符串 |
| locales/zh.ts | 新增约 130 个 key |
| locales/en.ts | 新增约 130 个 key |

### 新增 locale key 统计

| 命名空间 | 新增数量 | 说明 |
|----------|---------|------|
| confirm | 3 | confirm_text, cancel_text, processing |
| graph | 8 | 相对时间 + aria-label |
| search | 1 | decision_context_error |
| settings.diagnostics_* | 35+ | 诊断详情、组件标签、状态文案 |
| mcp.other_* | 7 | MCP 兜底卡片 |
| mcp.* (各客户端) | 50+ | reload_hint, steps, config_path |
| shortcuts | 10 | 8 个快捷键 name/desc + hardware |
| llm_presets | 17 | speed/cost/name 标签 |

## 残留

以下文件保留中文（运行时数据/开发者注释/配置默认值）：

- hooks/useOmniContext.ts: addLog 消息是运行时事件数据
- hooks/useKeyboardShortcuts.ts: 默认快捷键配置注释
- useTranslation.tsx: SSR 处理逻辑注释
- GraphViewer.tsx/page.tsx 等: 业务逻辑注释
- 所有 console.warn/error: 开发者日志

## 自测结果

- npx tsc --noEmit: 通过
- npm run build: 通过
- grep 硬编码中文（排除注释/console/locales）: src/lib/llm-presets.ts 仅剩文件头注释，其余文件 0 命中
