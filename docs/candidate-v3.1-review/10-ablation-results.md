# Ablation results

Status: **NOT_RUN**.

The requested Targeted ablations were authorized only after a passing complete mechanism run. The complete run failed at embedding preflight, so selector-off and grouping-off ablations were not run.
