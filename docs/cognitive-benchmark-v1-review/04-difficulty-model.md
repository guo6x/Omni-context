# Difficulty Model

难度只由场景设计因素决定，不由预计正确率决定：事件数、时间跨度、状态变化、冲突/噪声数量、推理跳数、约束数、当前/历史区分、跨 Agent 传播和主动洞察要求。

本草案按每类索引稳定分配 30% easy、50% medium、20% hard：40 题类别为 12/20/8，30 题类别为 9/15/6。当前模板的事件数变化不足，难度标签主要代表预留的结构层级；外部冻结前必须让 hard 场景实际包含更多冲突、噪声、时间点和组合约束，并再次审查分布。
